{% extends 'base.html.twig' %}

{% block title %}Ajouter une image{% endblock %}

{% block body %}
    <div class="form-container">
        <h1 class="title-licorice">Ajouter une image à la galerie</h1>
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-primary">Télécharger</button>
        {{ form_end(form) }}
    </div>

{% endblock %}
